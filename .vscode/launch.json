{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug client Windows",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/src"
        },
    ]
}